apiVersion: v1
kind: Service
metadata:
  name: "pokemon"
spec:
  selector:
    app: "pokemon-api"
  ports:
    - protocol: "TCP"
      port: 10080
      targetPort: 80
      nodePort: 31000
  type: NodePort

